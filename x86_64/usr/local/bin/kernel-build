#!/bin/bash

P=$*

[ "$P" == "all" ] && rm -rf /lib/modules/* /boot/*
[ "$P" == "kernel" ] && rm -rf /lib/modules/* /boot/*

genkernel --makeopts=-j64 --no-keymap --no-zfs --no-btrfs --menuconfig --no-mrproper --no-clean --initramfs-overlay=/usr/src/initramfs $P
